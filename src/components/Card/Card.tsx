import "./Card.scss";
// Render a card

interface CardProps {
  selected: boolean;
  disabled: boolean;
  boardDisabled: boolean;
}

const Card = (props: CardProps) => {
  return <div></div>;
};

export default Card;
